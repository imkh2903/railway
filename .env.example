# Railway production variables
NODE_ENV=production
# Railway injects PORT automatically. Do not hard-code it.
# Attach a Railway Volume mounted at /data.
DATA_DIR=/data

# Optional tuning
SESSION_TTL_HOURS=8
MAX_UPLOAD_MB=10
